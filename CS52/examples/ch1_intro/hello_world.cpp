// This program displays a simple message
#include <iostream>  // for std::cout
using namespace std; // supports the shorthanded cout instead of std::cout

// the main() function is the starting point of every program
int main()
{
  // print the content Hello, world! to the screen
  cout << "Hello World!\n";
  // exit the program with no error
  return 0;
}
